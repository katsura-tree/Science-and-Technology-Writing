# CLAUDE.md

이 저장소는 과학기술글쓰기 과목에서 쓴 글 모음이다. 사용자가 글을 써 달라고 하면 **사용자의 문체를 따라** 쓴다.

## 글을 써 달라는 요청을 받으면

1. 먼저 [`style-guide.md`](style-guide.md)를 읽는다.
2. 문체 기준 텍스트를 읽는다. 각 `writings/*/README.md`의 "본문" 중 **작성자가 제공한 원본 텍스트**만 기준으로 삼는다.
   - `transcription-claude.md`나 Claude가 사진을 보고 옮긴 본문은 문체 기준으로 쓰지 않는다.
   - `drafts/`의 글은 Claude가 쓴 초안이므로 문체 기준으로 쓰지 않는다.
3. 문체(말투, 문장 길이, 문단 구성, 논리 전개, 표기 습관)는 원본을 따른다. `style-guide.md`의 "피할 것"에 적힌 첨삭 지적은 되풀이하지 않는다.
4. 분량, 주제, 글의 종류(주장하는 글인지 경험을 쓰는 글인지), 원고지 형식 여부가 요청에 없으면 쓰기 전에 사용자에게 묻는다.

## 새 글을 추가할 때

- `writings/NN-짧은-영문-이름/`에 원본 사진, 본문과 첨삭을 담은 `README.md`를 둔다. 맨 위 `README.md` 목록에도 추가한다.
- 사진을 보고 옮긴 텍스트는 판독본으로 표시한다. 사용자가 원본 텍스트를 주면 그것을 본문으로 쓰고, 판독본은 `transcription-claude.md`로 따로 둔다.
- 원본 텍스트가 추가되면 `style-guide.md`도 갱신한다.

## Claude가 쓴 초안

- 사용자 요청으로 쓴 글은 `drafts/NN-짧은-영문-이름.md`에 "Claude 초안"이라고 표시해서 저장하고, 맨 위 `README.md`의 초안 목록에 추가한다.
- 사용자가 초안을 다듬으면 같은 파일에 최종본, 사용자가 다듬은 글(원문 그대로), Claude 초안을 함께 남기고 "Claude가 초안을 쓰고 작성자가 다듬은 글"이라고 표시한다. Claude의 문장이 섞여 있으므로 문체 기준에는 넣지 않는다. 다만 사용자가 무엇을 어떻게 고쳤는지는 문체를 파악하는 참고 자료로 쓸 수 있다.
